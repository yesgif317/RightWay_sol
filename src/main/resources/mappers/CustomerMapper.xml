<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mark.mappers.CustomerMapper">

    <!--Customer-->
    <!-- Select 문 -->
    <select id="selectCustomer" resultType="CustomerVO">
        SELECT * FROM customer
    </select>

    <!-- 회원가입(Insert) -->
    <insert id="insertCustomer" parameterType="CustomerVO" >
        insert into customer(c_id, c_pwd, c_name, c_email, c_phone,c_dep,c_position,com_num,c_state)
        values (#{c_id}, #{c_pwd}, #{c_name},#{c_email},#{c_phone},#{c_dep},#{c_position},#{com_num},#{c_state})
    </insert>

</mapper>